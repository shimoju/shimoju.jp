# 7. テーマ実装の開始用プロンプト

状態：確定（2026-09-17）。以下の本文をCodexの`/goal`へ渡す。この文書の確定自体はテーマ実装の開始を意味しない。

```text
Hugoブログテーマshshを実装し、このサイトをdocs/04-theme-implementation-requirements.mdの移行完了条件を満たす状態まで移行してください。初回実装で終了せず、レビューでの指摘・ユーザーの判断・修正・再検証まで進めてください。

最初にAGENTS.md、docs/03-theme-visual-requirements.md、docs/04-theme-implementation-requirements.md、docs/05-theme-implementation-discipline.md、docs/06-theme-implementation-progress.json、mock/README.mdを読み、モックと現在のリポジトリを確認してください。資料5を実装中の規律とし、進捗JSONを再開の起点にしてください。

新規開始なら、資料4の全要件・補足・完了条件と資料5の規律から、一つの目的と検証可能な成果を持ち、関連差分をまとめてレビューできるタスクへ分割してください。要件番号やファイル単位で機械的に分けず、関連する実装・検査・文書をまとめ、原則として一つのタスクが一つのコミットになる粒度にしてください。

各タスクに、対応要件、対象範囲、依存先、完了条件、具体的な検証方法、タスクIDを含む日本語の予定コミットメッセージを記録してください。代表画面のレビュー、承認後の全画面への展開、移行照合、実機・配信検証までをJSON上で対応付け、未割当の要件・検証漏れ・依存関係の循環がないことを確認してください。計画をT001としてコミットしてから実装へ進み、必要に応じて要件の対応を保ったまま分割を見直してください。

各タスクで必要な検証を行い、関連する実装・検査・文書と進捗JSONを一緒にコミットしてください。メッセージは「[T001] テーマ実装計画と検証条件を定義」の形式とし、Git履歴からタスクを追跡できるようにしてください。レビュー待ちの成果は状態を正確に記録してコミットし、コミットと完了判定を混同しないでください。レビューで生じた修正は後続タスクとしてコミットしてください。

再開なら、JSONの状態を実際の成果物とGit履歴に照合し、未完了・判断待ち・再検証待ちから続けてください。作業単位の終了時、検証後、判断後、中断前にJSONを更新し、次に行うことを明記してください。

モックは表示・操作の比較基準として凍結し、実装構造は役割と責務に基づいて設計してください。同じ役割を一緒に変更できるCSSトークン・レイアウト・テンプレートを用い、CSS・TypeScriptを適切に分割して、資料4のHugo Pipes構成で処理してください。数値の一致だけによる共通化や、根拠のない個別調整を避けてください。

表示・操作を維持する内部整理と合意済みの変更は自律的に進めてください。未合意の見た目・操作の変更や仕様の矛盾を見つけた場合は、現状・根拠・影響・推奨案を指摘として記録し、ユーザーに判断を求めてください。重大な矛盾は発見時に提示し、判断待ちでも依存しない作業は続行してください。

代表画面の段階で、両配色・PC／モバイルの表示比較とトークン・CSS・TypeScript・テンプレートの構造をレビューに出し、共通設計の承認後に残りの画面へ展開してください。全画面の段階でも、一貫性・検証結果・残件をレビューに出してください。承認した変更は要件文書とJSONに反映し、期待値の更新と影響範囲の再検証を行ってください。

資料4で要求する自動検査・視覚比較・移行照合・実機確認・性能測定・配信環境の検証を行い、対象・環境・手順・証拠を記録してください。人の判断・操作・確認が必要な工程は、確認可能な成果物と手順を用意して依頼してください。未実施や確認待ちを合格・完了扱いせず、要件や検査を黙って弱めないでください。

資料4の移行完了条件と資料5のレビュー・修正・再検証を満たしてから完了としてください。最終報告には実装結果、検証の証拠、承認済みのモックとの差分、残る制約を簡潔にまとめてください。
```
