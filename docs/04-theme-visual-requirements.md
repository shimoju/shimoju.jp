# 4. 自作テーマの視覚要件

初回合意：2026-09-06。grillingによるQ1〜Q36の合意に、同日のHTMLモックレビューを反映した現行要件。初版の比較過程は[資料5](05-theme-mock-review.md)、今回の変更・意見・検証は[資料6](06-theme-mock-revision.md)、閲覧方法は[モックREADME](../mock/README.md)を参照。

## 位置付けとデザインの軸

日本語の技術文書を読みやすくし、技術記事以外の日記・写真にも適用できる自作テーマを目指す。PaperModからは一列構成、控えめな装飾、本文への集中を引き継ぐ。カード枠・影・面分けは引き継がない。

- 本文は日本語ゴシック体。Webフォント・明朝体は採用しない。
- 文字・余白・階調を揃える。通常の見出しに下線・背景色・色付きの縦線を付けない。
- ライト／ダークを同等に設計する。コードブロックはCatppuccin Latte／Mochaの公式配色を維持する。
- サイドバー、固定ヘッダー、固定目次、トップに戻るボタン、装飾的なアニメーションは設けない。
- 採用済み項目はモックの選択肢から外す。比較対象と、調整可能な初期値を区別する。

今回の成果物は静的HTMLモックと視覚要件。Hugoテーマの完成実装、SEO・性能、全記事移行、公開・外部サービス接続は別工程。資料1〜3は調査資料であり、採否については本資料を優先する。

## 対象画面・状態・幅

| 対象 | 内容 |
| --- | --- |
| 記事詳細 | デザインの基準。長文・短文、長いタイトル、カバーあり／なし、更新あり／なし、タグなし |
| ホーム | サイトタイトル、短い紹介、GitHub・X・About、新着順の記事、ページ送り |
| About | 詳しいプロフィールと全リンク。記事の日付・タグ・シェア・スター・前後記事は表示しない |
| 記事一覧 | 見出し、必要なら説明、共通の一覧、ページ送り |
| タグ／カテゴリ一覧 | 分類名と件数。塗りつぶしたチップではなく文字を中心に折り返す |
| 個別タグ／カテゴリ | 分類の見出し、必要なら説明、共通の一覧 |
| Archives | 年→月→タイトルと日付。要約・カバーなし |
| 404 | 控えめな見出しと説明、Home・Archivesへの復帰導線。巨大な404を主役にしない |

検索・Profileモードは実装しない。目次は不採用とし、比較ページも残さない。将来必要になった場合は改めて検討する。

一覧は初・途中・最終ページ、0件・1件・多数、画像・要約の有無を確認する。紹介文はホーム初ページのみ。ページ送りは存在する移動先だけを表示し、初ページのPreviousと最終ページのNextはラベルごと省く。1ページしかない場合はページ送り自体を省く。静的HTMLでもこの表示を実現する。

全画面を400px／1280px・両配色で確認し、320px・360px・768pxで収まりを追加確認する。400pxは代表幅であり最低幅やブレークポイントではない。実装上の切り替え幅は640pxを初期値とする。狭い幅や拡大相当の幅では本文を折り返し、コード・表だけを局所的に横スクロールさせる。

## 情報設計と共通UI

- 中央のサイトタイトルを主役にし、その下にAbout／Archives／Categories／Tagsと配色切り替えを置く。
- 記事タイトル・本文は左揃え。ホームに独立した著者名の見出しは置かない。
- 文脈から明らかな「新着記事」「公開」「シェア」の可視ラベルを省く。ホームのH1はサイトタイトルとする。
- 更新日には公開日との区別に必要なラベルを残す。
- フッターは著作権と「RSS」を基本にする。「RSSを購読」とは表記しない。RSSをSNSプロフィール群に混ぜない。
- ナビはモバイルでも隠さず折り返す。配色ボタンだけにホバー背景を付けることはしない。
- 読者向け操作語を統一する。今回のモックでは英語案（Previous / Next / Copy / Copied / Updated / RSS）を提示する。英語への最終統一は意見提示段階であり、記事本文とレビュー用説明は日本語のまま。
- OS設定を初期値とし、手動選択後はライト／ダークの選択を保持する。二状態ボタンとし、システム設定に戻すUIは作らない。次の切り替え先がアイコン・読み上げラベルで分かるようにする。

## タイポグラフィ

### 採用確定

| 項目 | 採用値 |
| --- | --- |
| 本文サイズ | デスクトップ17px、モバイル16px |
| 記事タイトル | 本文の1.6倍（27.2px／25.6px） |
| コード行高 | 1.5 |
| 本文リンク | 本文色＋下線。ホバーでBlueへ変更しない |

本文の左揃え、自然な和欧文混在、過度な字詰めをしないこと、一行を長くしすぎないことを必須にする。本文は最大720px・行高1.9・太さ400・段落間隔1emを初期値として維持する。

見出しの前を広く、後を狭くし、続く本文とまとめる。下位見出しを本文より小さくしない。

| 要素 | 本文に対する比率 |
| --- | ---: |
| H2 | 1.4 |
| H3 | 1.25 |
| H4 | 1.125 |
| H5 | 1.0625 |
| H6 | 1 |

本文見出しは太さ600・行高1.5を初期値とする。H5／H6の差は小さいため、長い見出しや前後の文章を含めて確認し続ける。

### システムフォント

端末間の字形は異なっても、読みやすさと階層を揃える。欧文候補を先、和文候補を後に置く初期案を維持する。

```css
--font-body: "Helvetica Neue", "Segoe UI", Roboto, Arial,
  "Hiragino Sans", "Hiragino Kaku Gothic ProN", "Yu Gothic", Meiryo,
  "Noto Sans CJK JP", "Noto Sans JP", sans-serif;
--font-code: "SFMono-Regular", Menlo, Consolas, "Liberation Mono",
  "BIZ UDGothic", "MS Gothic", monospace;
```

モックのレビュー画面に現在のfont-familyを表示する。CSS変数から取得し、候補順の説明と和欧文サンプルを添える。これは文字ごとに実際に使われたフォントの特定ではない。

CSSのフォント選択は半角／全角分類ではなく文字の収録状況に基づく。コードの日本語1文字が必ず欧文2文字分になるとは仮定しない。[CSS Fonts](https://www.w3.org/TR/css-fonts-4/#font-family-prop)

## 配色・コード・本文部品

| 用途 | ライト | ダーク |
| --- | --- | --- |
| ページ背景 | #f8f9fc（派生色） | #181825（Mantle） |
| コードブロック背景・固定 | #eff1f5（Base） | #1e1e2e（Base） |
| 本文 | #4c4f69 | #cdd6f4 |
| インラインコード背景・初期値 | #e6e9ef（Mantle） | #313244（Surface0） |

コードブロックのトークン色と背景は公式Latte／Mochaを維持する。サイト側の背景・境界・文字色には、コントラストや領域の区別に必要な範囲で派生色を許容する。インラインコードはレビューにより「Base固定」から変更し、本文背景との区別を強める。コードブロックの配色変更とは混同しない。

### コードブロック

- 14px・行高1.5、細い境界、小さな角丸、十分な内側余白を基本にする。
- 長い行は折り返さず、コード内で横スクロールする。コピーは常時表示する。
- 言語指定がある場合のみ言語を表示する。ファイル名は指定がある場合のみ表示し、本文から推測しない。
- Hugoのコードブロックレンダーフックで、言語は `.Type`、ファイル名は `.Attributes.filename` を取得する。フェンスの `filename="config.toml"` はHugoの属性記法を利用したカスタム表示であり、標準Markdownに自動表示機能があるという意味ではない。
- 言語もファイル名もない場合は空のラベル領域を置かず、上部の余白を狭くする。ただしタッチ操作できるコピー領域は確保する。
- 行番号は通常表示せず、指定時のみ対応する。言語なし、未対応言語、行番号、強調行も確認する。
- コピー成功時はボタンの表示だけを変更し、3秒後に元へ戻す。ブロック下部の可視メッセージは出さない。読み上げ用通知は視覚レイアウトを変えずに提供する。
- 失敗時は成功表示にせず、ボタンで失敗と再試行が分かるようにする。
- インラインコードは同じ等幅フォントを使い、本文の行高を押し広げない。

### その他の部品

| 部品 | ルール |
| --- | --- |
| 引用 | 細い左罫線と余白。背景色を変えない |
| 表 | 見出し行の太字と薄い横罫線。全セルの枠・縦罫線は使わない。必要時は局所スクロール |
| リスト | 通常の記号、項目間と入れ子の余白を整える |
| 画像 | 幅を基準に元の縦横比を維持し、コードと同様の角丸にする。高さ上限・切り抜き・影は付けない |
| キャプション | 画像下に左揃え、控えめな文字色とサイズ |
| 脚注 | 本文末にまとめ、参照元へのリンクを置く |
| タグリンク | #付き、枠・下線なし。文字と余白でリンクを示す |

## 記事詳細

記事タイトル → 日付（必要なら更新日）→ 任意のカバー → 本文 → タグ → シェア・はてなスター → 前後記事の順。

著者名の毎記事での反復、読了時間、文字数、パンくず、編集リンク、トップに戻るボタンは表示しない。Aboutには記事用の周辺情報を付けない。

シェア先はX・Facebook・はてなブックマーク。はてなスターはシェアと別の操作として区別する。Xのシェアにタグ由来のハッシュタグは付けない。モックでは送信しないローカルな状態表示とし、本実装でサービスへ接続する。

## 記事一覧・カバー・クリック範囲

一覧カバーはデスクトップでも本文幅いっぱいを採用する。横添え案は廃止。主比較素材は1200×630とし、720px幅なら高さ378px。高さを360pxなどに制限して幅まで縮める処理をしない。ほかの比率の画像も切り抜かない。

一記事につきタイトル・日付・要約（まず3行）と任意のカバーをまとめる。画像がどの記事に属するか分かるように、内側の間隔を小さく、記事間を広くする。第2案では記事間に細い区切り線も置く。囲みカードや塗りつぶし背景は作らない。

- ホームを含む共通一覧では、タイトル・日付・要約・画像を一つのリンクにする。
- Archivesではタイトルと日付を同じリンクに含める。
- タグ・カテゴリの分類名と件数も、余白込みで押せる範囲を確保する。
- 記事全体をクリック可能にした場合、タイトルのホバー下線は付けない。
- ネイティブのリンクを使い、新しいタブで開く操作、キーボード移動、フォーカスの識別を維持する。リンクの入れ子や、同じ記事への重複した停止位置を避ける。
- 押せる範囲を広げることだけで、アクセシビリティ全般への適合を保証しない。

## 今回残す比較項目

| 項目 | 基本案 | 比較案 |
| --- | --- | --- |
| 一覧カバーの位置 | タイトル・日付・要約の下 | タイトルの上 |
| サイトタイトル | ホーム36px／30px、記事等24px／22px | 全画面36px／30pxに統一。ヘッダー上余白も揃える |
| フッター | 著作権を左、RSSを右 | 両者を中央にまとめる |

全ページへの比較の総当たりは不要。カバーは画像あり／なしのホーム、サイトタイトルはホームと記事の往復、フッターは短いページと長いページで比較する。英語UI統一は提案として確認する。

## 検証と成果物

基本画面の両配色・基準幅、追加幅、通常／ホバー／キーボードフォーカス、コピー成功／復帰／失敗、配色切り替え、拡大相当のリフローを確認する。ホバーだけで存在が分かる操作にはしない。フォーカス輪郭は残す。

実記事を優先し、不足するH2〜H6、引用、長い表、脚注、和欧文混在、言語なし・日本語コメント付きコード、画像とキャプションは部品ページで補う。

成果物には、全対象画面、比較案、実際のデザイントークン、採否・継続検討の理由、ブラウザでの確認範囲と未確認事項を残す。別OS・別ブラウザやWCAGへの適合を、単一環境のモック検証だけで保証しない。

## 参考

- [riq0h](https://riq0h.jp/2026/07/21/194535/)：控えめなタイトル、見出し・本文・余白のバランス、サイト名の存在感。明朝体は採用しない。
- [Zenn](https://zenn.dev/zenn/articles/markdown-guide)、[Qiita](https://qiita.com/Qiita/items/c686397e4a0f4f11683d)：日本語の技術文書と本文部品。
- [Catppuccin Palette](https://catppuccin.com/palette/)：コードの固定色とサイト色の基準。
- [400px制作基準の提案](https://note.com/wurst/n/n377374731902)：代表幅の参考。このブログの読者分布を示すものではない。
- [W3C Reflow](https://www.w3.org/WAI/WCAG22/Understanding/reflow.html)：320 CSS px相当での読みやすさ。
- [Hugoのコードブロックレンダーフック](https://gohugo.io/render-hooks/code-blocks/)：言語・属性・ハイライトオプションの取得。

参考サイトの寸法はOS・閲覧幅・時期で変わる。本資料の数値はこのテーマの合意値と初期値であり、参考サイトの実測値を再現する指定ではない。
