shshの本番移行後に残っていたPaperMod submoduleと更新スクリプトを撤去し、MIT表示を保持します。表示・操作仕様の参照先をテーマ・fixture・文書へ移し、モックは固定の参考資料注記だけを追加しました。元のモック本文・描画ソースのハッシュ検査は維持しています。

T071の本番配信証拠（170 HTML・48 RSS・共有/スター・外部4種）と、T018の全要件監査・承認済み制約を同梱します。生成ソース・設定・記事・素材は本番 a442780 と同一です。

検証: 撤去後の `pnpm check` は3エンジン246テストを含め成功。Hugo 0.166.0単独の本番同等生成も成功。モック68ファイルの元バイト、移行基準JSON不変、MIT原文一致を確認しました。必須CI成功後に統合し、最終配信結果をT072で記録します。
