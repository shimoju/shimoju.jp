# T062: Safari実フォントの取得制約

2026-09-18、macOS27.0 / Safari27.0（22625.1.29.11.27）、CUAネイティブ操作。対象は固定preview6f52990f（268bbb0）のAbout。

専用タブを開き、職務要約の最初の本文段落を右クリック→「要素の詳細を表示」。Inspectorの選択要素が`div.prose > p`、右側の「フォント」が選択されていることをAX・画像で確認したが、パネル内は空だった。段落を展開して和文を含むテキストノードを選ぶと「ノード」へ切り替わり、親pへ戻るとFontsは再び空。実フォント名は取得できなかった。

`fonts-panel.txt`は親pへ戻った後のAX（行末空白のみ除去）、`fonts-panel.jpg`は同時点の画像。候補CSSや外観から実フェイスを推定しない。コードの追加取得は未実施。T027/T050の同じ制約が再現したため、操作を繰り返さずユーザーにAbout本文・開発環境記事コードの和欧文実フォント名、またはパネルが空かを確認依頼した。

Inspectorの閉じるボタンを押し、専用AboutタブをCmd+Wで閉じた。最後のAXで元の開発環境記事タイトルとURLへ戻ったことを確認。配色・文字設定・サイト入力は変更していない。

結果はblocked。ユーザーの回答を待ち、未取得を合格にしない。Safariの動画確認済み結果は維持する。
