# About列見出しの承認と反映（F014 / T026）

2026-09-18、ユーザーが「『業務経験／技術／経験年数』を採用する（推奨）」と回答したため、`content/about.md`の業務経験表の見出し1行へ反映した。経歴・技術・年数の行は変更していない。この判断はF014の採用で、R002全体の承認ではない。

## 比較・移行検査への反映

- 入力監査では旧Aboutに対する厳密な1行置換だけを許可する。旧80入力・素材のうち73はバイト不変、6件は既存の埋め込み移行、1件はF014となる。
- 本文照合ではAboutの「業務経験」の直後に承認した2ラベルを加えるだけで、本文・リンク・見出しID・素材・RSSを引き続き照合する。
- 全画面比較では凍結モックのAboutを読み込んだ後、ブラウザ内だけで空の2見出しへ承認ラベルを設定する。比較画像名は`mock-approved-f014`とし、元のモックとの差分を明示する。モック68ファイルのSHA-256検査は維持する。
- Aboutの空thをaxe例外にしない。3エンジン・両幅・両配色の検査を行い、修正後の画像を`t026/images/`へ保存する。R002の修正前画像・提案検査は履歴として保持する。

## 検証結果

macOS27 / Hugo0.166.0 / Node24.21.0 / pnpm12.4.1、Chromium153・Firefox155・WebKit26.6で実行。

- `pnpm check`は静的検査・Hugo生成・HTML/XML・3エンジンの**240件すべて合格（ブラウザ工程2.4分）**。[統合ログ](t026/check.log)
- Aboutは両幅・両配色で承認差分適用後のモックと寸法一致。axe違反0、文字拡大125/200%も横溢れなし。[寸法](t026/metrics/)、[axe](t026/axe/)
- 全入力の許可差分・170 HTML/48 RSS・64コード・142画像・凍結モック68ファイルを再照合。[入力監査](t026/input-migration.json)、[出力照合](t026/migration/)
- 本番同等production/previewを再生成し、両方Hugoエラー・警告0。[生成ログ](t026/live-build.log)
- 現状/比較用fixtureの24条件で列見出し一致・axe違反0・横溢れなし、レビュー入口の全リンクHTTP200。[結果](t026/review-verification.json)、[ログ](t026/review-check.log)

PC/モバイル両配色の表画像を目視確認した。モバイルの「フロントエンド」等はセル内で折り返し、全3列が収まる。R002入口は承認・反映後のAboutを示し、修正前画像と過去の不合格ログを履歴として保持する。
